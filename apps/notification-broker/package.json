{
  "name": "@opencode2-mobile/notification-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "opencode-mobile-notifications": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/cli.js serve",
    "test": "pnpm --filter @opencode2-mobile/notification-protocol build && vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@opencode2-mobile/notification-protocol": "workspace:*",
    "qrcode-terminal": "0.12.0"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "@types/qrcode-terminal": "0.12.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
